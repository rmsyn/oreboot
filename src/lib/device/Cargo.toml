[package]
name = "device"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
software_i2c = []

[dependencies]
bitfield = "0.14.0"
log = "0.4"
spin = "0.9.4"
util = { path = "../util" }
